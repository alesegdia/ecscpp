#include "GameWorld.h"

/*
void GameWorld::notifyAdded(eid_t e)
{
	for(System* sys : _systems)
	{
		sys->added(e);
	}
}

void GameWorld::notifyDeleted(eid_t e)
{
	for(System* sys : _systems)
	{
		sys->deleted(e);
	}
}
*/
